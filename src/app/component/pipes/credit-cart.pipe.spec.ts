import { CreditCartPipe } from './credit-cart.pipe';

describe('CreditCartPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditCartPipe();
    expect(pipe).toBeTruthy();
  });
});
