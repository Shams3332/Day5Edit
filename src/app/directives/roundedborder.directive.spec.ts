import { RoundedborderDirective } from './roundedborder.directive';

describe('RoundedborderDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedborderDirective();
    expect(directive).toBeTruthy();
  });
});
